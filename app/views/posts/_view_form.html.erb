<!--remote: form is loaded with javascript so we dont have to reload the page-->
<%= form_with(url: post_view_path, html: { id: "save_views" }) do |form| %>
  <%= form.hidden_field :id, value: post.id %>
<% end %>